# 智眸任务获取和返回数据接口

## 1. 获取待识别任务

```http
GET /intellisight_mo/api/get_task.php
```

也支持 `POST`。接口按任务编号顺序派发状态为“待识别”的任务，同一时间只允许一个任务处于“识别中”。

### 有任务响应

```json
{
  "success": true,
  "has_task": true,
  "task_id": 26,
  "task": {
    "task_id": 26,
    "status": 3,
    "status_text": "识别中",
    "created_at": "2026-09-17 10:00:00",
    "created_by_mail": "user@example.com",
    "contract_quote_epo_file": "document.pdf",
    "contract_quote_epo_url": "https://example.com/intellisight_mo/api/download_file.php?id=26"
  }
}
```

`task_id` 是数值型任务 ID，为方便对接程序与其他任务接口使用一致的顶层字段，同时也保留在 `task.task_id` 中。调用方应先判断 `has_task`；无任务时响应不包含 `task_id`：

```json
{"success":true,"has_task":false,"message":"暂无待识别任务"}
```

## 2. 返回识别数据

```http
POST /intellisight_mo/api/returndata.php
Content-Type: application/json
```

请求可直接传以下对象，也可以使用 `{ "data": { ... } }` 包裹：

```json
{
  "task_id": 26,
  "po_no": "PO-2026-001",
  "customer_name": "ABC Company",
  "customer_delivery_address": "Macau",
  "end_user_name": "End User Ltd.",
  "end_user_contact": "Chan Tai Man",
  "end_user_email": "contact@example.com",
  "vendor_part_no": "PC121231;PC321442",
  "description": "AAA;BBB",
  "qty": "2;3",
  "price_currency": "MOP;USD",
  "unit_price": "100.00;25.50"
}
```

### 字段说明

| 字段 | 说明 |
|---|---|
| `task_id` | 必填，获取任务接口返回的数值型任务 ID |
| `po_no` | PO 编号 |
| `customer_name` | 客户名称 |
| `customer_delivery_address` | 客户送货地址 |
| `end_user_name` | 最终用户名称 |
| `end_user_contact` | 最终用户联系人 |
| `end_user_email` | 最终用户邮箱 |
| `vendor_part_no` | 多值，英文分号分隔 |
| `description` | 多值，英文分号分隔 |
| `qty` | 多值，英文分号分隔 |
| `price_currency` | 多值，英文分号分隔 |
| `unit_price` | 多值，英文分号分隔 |

五个多值字段的值按位置一一对应，项目数量必须相同。例如 `PC121231` 对应 `AAA`、`2`、`MOP` 和 `100.00`。接口也接受这五个字段使用 JSON 数组传值，保存和发送至 P 系统时会统一为分号分隔字符串。

识别结果保存后，系统立即以同一套字段和格式请求 P 系统的 `POST /api/tasks`。P 系统明确返回 `success: true` 后，任务状态变为 `4`（匹配中）。默认服务地址为 `http://10.106.4.46:12332`；可用环境变量 `P_SYS_PORT` 修改端口，或用 `P_SYS_ENDPOINT` 覆盖完整任务接收地址。

### 成功响应

```json
{
  "success": true,
  "message": "识别结果已保存并成功请求 P 系统。",
  "task_id": 26,
  "status": 4,
  "status_text": "匹配中",
  "p_system_response": {
    "success": true,
    "message": "任务已接收。",
    "task_id": 26
  }
}
```
